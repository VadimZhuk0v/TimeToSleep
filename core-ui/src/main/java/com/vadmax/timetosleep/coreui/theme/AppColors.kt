package com.vadmax.timetosleep.coreui.theme

import androidx.compose.ui.graphics.Color

object AppColors {
    val permissionGrant = Color(0xFF019E01)
    val cameraFrame = Color.White
    val warning = Color(0xFFE39D00)
    val warningRed = Color(0xFFAD0000)
    val download = Color(0xFF007BFF)
}
